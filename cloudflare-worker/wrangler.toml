name = "bass-verify"
main = "worker.js"
compatibility_date = "2024-09-23"

[vars]
# Slack channel ID to post Confirm & Flag notifications to (e.g. "C0123456789").
# Not sensitive — the bot token (which is) goes in a secret instead:
#   wrangler secret put SLACK_BOT_TOKEN
SLACK_CHANNEL_ID = "C0BV91K7F70"
