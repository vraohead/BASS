/**
 * CONFIG.JS - Zendesk Credentials Configuration
 * 
 * THIS FILE IS NOT USED CURRENTLY.
 * 
 * To configure credentials, edit the ZENDESK_CONFIG object in background.js:
 * 
 * Open background.js and find this section:
 * 
 *   let ZENDESK_CONFIG = {
 *     subdomain: 'your-subdomain',      // e.g., 'headout'
 *     email: 'your-email@company.com',   // e.g., 'api@headout.com'
 *     token: 'your-api-token'            // Your Zendesk API token
 *   };
 * 
 * Replace the placeholder values with your actual Zendesk credentials.
 * Then reload the extension in Chrome.
 */

